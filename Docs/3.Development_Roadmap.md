# LLM IDE Development Roadmap v0.1

## 1. 문서 목적

이 문서는 LLM IDE의 개발 단계를 관리하기 위한 실행 로드맵이다.

이 문서는 다음을 다룬다.

* MVP 개발 순서
* Phase별 목표
* Phase별 구현 항목
* Phase별 완료 기준
* 현재 진행 상태
* 다음 작업
* 후속 확장 계획

제품 원칙과 아키텍처 기준은 별도 문서에서 관리한다.

---

## 2. 현재 개발 기준

현재 개발 기준선:

```text
LLM IDE Project Folder Structure v0.5
```

현재 구현 방향:

```text
완료 = CLI 기반 Core + Agent Loop v1 + WPF GUI
1차 제품 시나리오 = AI 글쓰기 도우미 (Writing Workspace, DEC-067)
이후 단계 = Writing Workspace 중심 재정렬 (DEC-068)
후속 확장 = Local Server 적용
```

현재 핵심 흐름:

```text
Project
→ Provider
→ Conversation
→ Criteria
→ 프로젝트 상태
→ Rolling Context Compression
→ 맥락 빌더
→ 산출물
→ Agent Loop (읽기 전용 도구)
→ WPF
→ AI Writing Assistant (조사 → 초안 → 리뷰 → 파일)
→ Local Server
```

---

## 3. 전체 Phase 구성

```text
Phase 1. 프로젝트 초기화 및 프로젝트 목록 관리
Phase 2. Provider 기반 대화
Phase 3. 대화 로그 저장
Phase 4. Criteria 관리
Phase 5. 프로젝트 상태 관리
Phase 6. Rolling Context Compression
Phase 7. 맥락 빌더 v1
Phase 8. 산출물 저장
Phase 8.5. Agent Loop v1 - Search and Fetch Tools
Phase 9. WPF GUI 적용
Phase 10. AI Writing Assistant 방향 전환
Phase 11. Writing Project State / Plan
Phase 12. Research / Source Workflow
Phase 13. Draft / Artifact Writing Workflow
Phase 14. Writing Review / Revision Workflow
Phase 15. Writing File Workflow
Phase 16. Writing State Candidate
Phase 17. Local Server 적용
```

---

## 4. 현재 진행 상태

```text
Phase 1 완료
Phase 2 완료
Phase 3 완료
Phase 4 완료
Phase 5 완료
Phase 6 완료
Phase 7 완료
Phase 8 완료
Phase 8.5 완료
Phase 9 완료

현재 다음 작업:
Phase 10. AI Writing Assistant 방향 전환
```

---

# Phase 1. 프로젝트 초기화 및 프로젝트 목록 관리

## 목표

프로젝트를 영어 `pID` 기반으로 초기화하고, 사용자 표시 이름 `name`은 별도로 저장한다.
IDE 프로그램 하부의 고정 프로젝트 저장소에서 프로젝트 목록을 관리한다.

## 구현 항목

* IDE 프로그램 루트 확인
* `<IdeProgramRoot>/project` 폴더 생성
* `<IdeProgramRoot>/project/projects.json` 생성
* 프로젝트 `pID` 필수 처리
* 빈 `pID` 입력 시 `DefaultProject`로 처리
* 기존 `DefaultProject` 중복 생성 차단
* 중복 오류 시 파일 저장 및 폴더 생성 금지
* 프로젝트 `pID` 중복 확인
* 프로젝트 `pID`는 영문자로 시작하고 영문자, 숫자, `-`, `_`만 허용
* 사용자 표시 이름 `name` 별도 저장
* 경로 미지정 시 `<IdeProgramRoot>/<ProjectPId>` 생성
* 경로 지정 시 지정 경로에 프로젝트 폴더 생성
* 프로젝트 `pID`, 사용자 표시 이름, 경로, 생성일자 저장
* 프로젝트 사용자 표시 이름 수정
* 프로젝트 `pID` 수정
* 프로젝트 경로 수정
* `.llmide` 폴더 생성
* `init-progress.json` 생성 및 단계별 진행 상태 기록
* 기본 설정 파일 생성
* `project.json` 생성
* `project-state.json` 생성
* `criteria.json` 생성
* `<IdeProgramRoot>/policies/` 기본 정책 템플릿 생성
* 신규 프로젝트 생성 시 `.llmide/policies/system-rule.md` 기본값 생성
* 신규 프로젝트 생성 시 `.llmide/policies/compression-rule.md` 기본값 생성
* 신규 프로젝트 생성 시 `.llmide/policies/artifact-rule.md` 기본값 생성
* 신규 프로젝트 생성 시 `.llmide/policies/importance-rule.md` 기본값 생성
* 신규 프로젝트 생성 시 `.llmide/policies/context-policy.json` 기본값 생성
* 신규 프로젝트 생성 시 `.llmide/policies/provider-policy.json` 기본값 생성
* 프로젝트 목록 조회
* 프로젝트 `pID`로 열기
* 프로젝트 `pID`로 목록에서 제거
* 프로젝트 `pID`로 실제 프로젝트 폴더 삭제
* 실제 프로젝트 폴더 삭제 시 프로젝트 `pID` 재확인 강제
* API Key가 있는 프로젝트 삭제 시 추가 확인 강제
* 권한 오류 표시 및 중단
* 옵션 없이 실행 시 사용법 출력
* CLI 명령 구현

## CLI 예시

```bash
llmide
llmide init
llmide init --pid MyProject
llmide init --pid MyProject --name "내 프로젝트"
llmide init --path /Users/me/Projects/DefaultProject
llmide init --pid MyProject --name "내 프로젝트" --path /Users/me/Projects/MyProject
llmide projects list
llmide projects open MyProject
llmide projects remove MyProject
llmide projects delete MyProject --confirm MyProject
llmide projects delete MyProject --confirm MyProject --confirm-api-key-delete
llmide projects rename MyProject "새 표시 이름"
llmide projects repid MyProject NewProjectId
llmide projects move MyProject /Users/me/Projects/NewPath
```

## 완료 기준

* `pID` 없이 초기화하면 `DefaultProject`로 해석된다.
* 기존 `DefaultProject`가 없는 경우 `DefaultProject`가 생성된다.
* 기존 `DefaultProject`가 있는 상태에서 `pID` 없이 초기화하면 오류를 표시하고 중단한다.
* 중복 오류에서는 파일 저장과 폴더 생성이 발생하지 않는다.
* 경로 없이 초기화하면 `<IdeProgramRoot>/<ProjectPId>` 아래에 프로젝트 데이터가 생성된다.
* `pID`, 표시 이름, 경로를 모두 지정하면 `pID`와 표시 이름은 고정 프로젝트 저장소에 등록되고 데이터는 지정 경로에 생성된다.
* 프로젝트 저장소에는 `pID`, 표시 이름, 경로, 생성일자가 저장된다.
* 프로젝트 `pID`, 표시 이름, 경로를 수정할 수 있다.
* 프로젝트 목록은 `pID` 기준으로 조회된다.
* 기존 프로젝트를 `pID`로 열 수 있다.
* `remove`는 프로젝트 저장소 목록에서만 제거하고 실제 프로젝트 폴더는 삭제하지 않는다.
* `delete`는 실제 프로젝트 폴더를 삭제하고 프로젝트 저장소 목록에서도 제거한다.
* `delete`는 `--confirm <project-id>` 없이는 실행되지 않는다.
* 프로젝트에 API Key가 있으면 `delete`는 `--confirm-api-key-delete` 없이는 실행되지 않는다.
* 권한 오류가 발생하면 오류를 표시하고 초기화를 중단한다.
* 프로젝트 폴더 생성 후 실패한 경우 `init-progress.json`에서 실패 단계를 확인할 수 있다.
* 옵션 없이 실행하면 사용법이 출력된다.
* 사용법에는 가능한 명령 조합이 출력된다.
* IDE 재실행 후에도 프로젝트 `pID`와 표시 이름 목록이 유지된다.

## 상태

```text
완료
```

---

# Phase 2. Provider 기반 대화

## 목표

DeepSeek API를 이용한 기본 대화를 구현한다.

## 구현 항목

* Provider 인터페이스 정의
* DeepSeek Provider 구현
* API Key 관리
* API Key 기본값은 빈 문자열로 생성
* Provider 모델 목록 조회
* 프로젝트 기본 모델 교체
* 요청/응답 처리
* 대용량 응답 스트리밍 출력
* Markdown 표와 코드 블록 원문 출력
* CLI 채팅 명령 구현

## CLI 예시

```bash
llmide chat MyProject "안녕"
llmide models list MyProject
llmide models set MyProject deepseek-reasoner
```

## 완료 기준

* 프로젝트 생성 시 `.llmide/settings/providers.json`에 DeepSeek 설정이 생성된다.
* DeepSeek API Key 값은 빈 문자열로 생성된다.
* DeepSeek API에서 사용 가능한 모델 목록을 조회할 수 있다.
* 프로젝트 기본 모델을 CLI에서 교체할 수 있다.
* 사용자 메시지를 전송할 수 있다.
* AI 응답을 받을 수 있다.
* 긴 응답은 도착하는 대로 CLI에 출력된다.
* 표와 코드 블록은 Markdown 원문 형식으로 출력된다.

## 상태

```text
완료
```

---

# Phase 3. 대화 로그 저장

## 목표

모든 대화를 프로젝트에 저장한다.

SQLite를 프로젝트 단위 주 저장소로 사용하고, JSONL/JSON 파일은 보조 기록, 디버깅, 요청 재현용으로 유지한다.

## 구현 항목

* `.llmide/conversations/conversation.db` 생성
* SQLite `conversation_turns` 테이블 저장
* SQLite `conversation_messages` 테이블 저장
* SQLite `context_packages` 테이블 저장
* `messages.jsonl` 저장
* `requests.jsonl` 저장
* 요청별 맥락 패키지 저장
* chat 명령 실행 시 사용자 메시지 저장
* chat 명령 응답 수신 시 assistant 메시지 저장
* 다음 chat 명령 실행 시 SQLite에 저장된 최근 사용자/AI 메시지 주입
* 요청 재현을 위한 `request_id` 연결
* SQLite 기록과 JSONL/JSON 보조 기록이 같은 `request_id`를 공유하도록 처리

## 완료 기준

* 프로젝트 초기화 시 `.llmide/conversations/conversation.db`가 생성된다.
* 사용자 메시지와 AI 응답이 저장된다.
* 사용자 메시지와 AI 응답이 SQLite의 같은 conversation turn에 연결된다.
* 요청별 맥락 패키지가 SQLite와 JSON 파일 양쪽에 저장된다.
* 두 번째 이후 대화는 이전 사용자/AI 메시지를 포함해 Provider API에 요청한다.
* 어떤 맥락으로 응답이 생성되었는지 추적 가능하다.
* `conversation_turns`, `conversation_messages`, `context_packages`가 `request_id`로 연결된다.
* `messages.jsonl`, `requests.jsonl`, `context-packages/<request_id>.json`이 서로 같은 `request_id`로 연결된다.
* 신규 대화 저장 시 request_id와 message_id는 프로젝트별 순번 기반으로 생성되어 저장 순서를 사람이 확인할 수 있다.
* 신규 chat request record에는 LLM이 평가한 `importance_weight`가 `0~10` 범위로 저장된다.

## 상태

```text
완료
```

---

# Phase 4. Criteria 관리

## 목표

프로젝트 기준을 저장하고 요청에 주입한다.

## 구현 항목

* Criteria 생성
* Criteria 수정
* Criteria 삭제
* Criteria 활성화
* Criteria 비활성화
* `criteria.json` 저장
* 활성 기준 주입
* 요청별 맥락 패키지에 활성 기준 기록
* 활성 기준가 없으면 chat 요청 중단
* CLI 관리 명령 구현

## CLI 예시

```bash
llmide criteria add MyProject --title "Korean" --description "항상 한국어로 답한다" --priority high
llmide criteria list MyProject
llmide criteria update MyProject <criterion-id> --description "한국어를 기본으로 사용한다"
llmide criteria deactivate MyProject <criterion-id>
llmide criteria activate MyProject <criterion-id>
llmide criteria remove MyProject <criterion-id>
```

## 완료 기준

* 활성 기준이 API 요청에 포함된다.
* 활성 기준은 system 메시지로 Provider 요청에 포함된다.
* 비활성 기준은 Provider 요청에 포함되지 않는다.
* 활성 기준이 하나도 없으면 Provider API로 전송하지 않고 중단한다.
* Criteria를 생성, 조회, 수정, 삭제할 수 있다.
* Criteria를 활성화, 비활성화할 수 있다.
* 요청별 맥락 패키지에서 사용된 활성 기준을 확인할 수 있다.
* 요청 로그에서 사용된 기준을 확인할 수 있다.

## 상태

```text
완료
```

---

# Phase 5. 프로젝트 상태 관리

## 목표

프로젝트 상태를 별도로 저장한다.

## 구현 항목

* Stage 관리
* Current 작업 관리
* Completed Items 관리
* Next Actions 관리
* Blockers 관리
* CLI 상태 관리 명령 구현

## 완료 기준

* 프로젝트 상태를 저장하고 수정할 수 있다.
* API 요청 시 프로젝트 상태가 포함된다.

## 상태

```text
완료
```

---

# Phase 6. Rolling Context Compression

## 목표

장기 대화 맥락을 롤링 맥락 요약로 관리한다.

압축은 LLM Provider API를 이용한다.
원본 대화 로그와 압축 맥락을 분리해 저장한다.

## 구현 항목

* `conversations/rolling-context/` 폴더 생성
* `rolling-context/current.md` 생성
* `rolling-context/history/` 생성
* `rolling-context/index.jsonl` 생성
* `policies/compression-rule.md` 생성
* `context-policy.json` 압축 정책 확장
* SQLite `rolling_context_summaries` 테이블 추가
* chat 응답 이후 압축 요청 실행
* 롤링 맥락 요약가 없는 최초 compression에서는 누적 원본 대화 로그 전체를 압축
* 이전 Summary와 현재 사용자/AI 대화를 병합
* debug 모드에서는 압축 요청 입력을 먼저 출력하고 Compression 응답을 스트리밍으로 출력
* 새 Summary를 `history/`에 저장
* `current.md` 갱신
* 기존 Summary를 `superseded` 상태로 기록
* 압축 요청를 Request Log에 별도로 기록
* Compression 실패 시 기존 Summary 유지

## 완료 기준

* chat 이후 롤링 맥락 요약가 생성된다.
* 다음 chat 요청에 `current.md`가 포함된다.
* 원본 대화 로그는 삭제되거나 덮어써지지 않는다.
* 압축 요청가 일반 채팅 요청와 구분되어 기록된다.
* 압축 요청의 실제 provider messages를 debug와 context package로 확인할 수 있다.
* 스트리밍 chat debug에서 Compression 응답 진행 상태를 chunk 단위로 확인할 수 있다.
* Compression 실패 시 사용자 응답은 정상 유지된다.
* Compression 실패 시 기존 Summary가 유지된다.

## 상태

```text
완료
```

---

# Phase 7. 맥락 빌더 v1

## 목표

요청에 필요한 맥락을 조립한다.

압축된 롤링 맥락 요약와 현재 발화 중심의 요청 조립을 적용한다.

## 구현 항목

* 시스템 규칙 주입
* 활성 기준 주입
* 프로젝트 상태 주입
* 롤링 맥락 요약 주입
* 최근 대화 창는 현재 MVP에서 `0`으로 기록
* 수동 첨부 필드 예약
* 산출물 필드 예약
* 맥락 패키지 생성
* 요청별 사용된 `rolling_context_id` 기록
* 최근 대화 포함 개수와 문자 수 제한 적용

## 완료 기준

* 요청마다 맥락 패키지가 생성된다.
* 동일한 요청을 재현할 수 있다.
* 과거 전체 대화 원문은 요청에 포함되지 않는다.
* 롤링 맥락 요약와 현재 사용자 발화가 함께 주입된다.
* 맥락 패키지에서 어떤 Summary와 최근 대화가 사용되었는지 확인할 수 있다.

## 상태

```text
완료
```

---

# Phase 8. 산출물 저장

## 목표

중요한 응답을 재사용 가능한 산출물로 저장한다.

AI 응답 중 보존 가치가 있는 내용을 명시 태그 기반 산출물 후보로 분리한다.
C# 코드는 자연어 의미를 추측하지 않고, 명시 태그가 붙은 영역만 추출한다.

## 설계 원칙

* chat 요청에는 `.llmide/policies/artifact-rule.md` 내용을 읽어 “답변 중 중요한 산출물 후보에는 산출물 태그를 붙이라”는 지시를 포함한다.
* 산출물 태그 형식은 구현 시점에 결정하되, MVP에서는 사람이 읽을 수 있고 파싱이 단순한 형식을 우선한다.
* 태그가 붙은 내용은 저장 후보일 뿐이며, 사용자 승인 없이 확정 저장하지 않는다.
* 원본 chat 응답은 그대로 대화 로그에 남긴다.
* 산출물 저장은 Agent Boundary Principle을 따른다.
* 산출물 태그 요청 문구는 코드 내부에 두지 않고 정책 파일로 관리한다.
* 신규 프로젝트 생성 시 `<IdeProgramRoot>/policies/`의 기본 정책 템플릿 전체를 `<ProjectRoot>/.llmide/policies/`로 복사한다.

## 구현 항목

* 산출물 태그 지시를 `policies/artifact-rule.md`에서 읽어 맥락 빌더가 주입
* 산출물 태그 파서 구현
* 산출물 후보 추출
* 산출물 생성
* 산출물 수정
* 산출물 목록 조회
* 산출물 첨부
* CLI 관리 명령 구현

## CLI 명령

```bash
llmide artifacts list <project-id>
llmide artifacts show <project-id> <artifact-id>
llmide artifacts add <project-id> --title <title> --type <type> --content <content>
llmide artifacts add <project-id> --title <title> --type <type> --file <file-path>
llmide artifacts update <project-id> <artifact-id> --title <title>
llmide artifacts update <project-id> <artifact-id> --content <content>
llmide artifacts remove <project-id> <artifact-id>
llmide artifacts extract <project-id> --content <response-text>
llmide chat <project-id> <message> --artifact <artifact-id>
```

## 완료 기준

* AI가 중요한 응답 영역에 산출물 태그를 붙일 수 있다.
* CLI가 태그가 붙은 산출물 후보를 추출할 수 있다.
* 응답을 산출물로 저장할 수 있다.
* 산출물를 이후 요청에 첨부할 수 있다.
* 태그가 없는 일반 응답은 산출물로 자동 분류하지 않는다.
* 사용자 승인 없이 산출물를 확정 저장하지 않는다.

## 상태

```text
완료
```

---

# Phase 8.5. Agent Loop v1 - Search and Fetch Tools

## 목표

사용자의 단일 Request를 처리하기 위해 LLM과 도구가 다회차로 주고받는 내부 실행 루프(Agent Loop)를 구현한다.
MVP에서는 읽기 전용 검색/조회 도구(`web_search`, `fetch_url`)만 제공한다.

## 위치 및 경계 (DEC-049)

Agent Loop는 Project Runtime의 하위 실행 루프다.

```text
사용자 Request 1회
→ Agent Loop (LLM ↔ ToolHost 다회차)
→ 최종 응답
= Request 처리 단위
```

* Agent Loop는 사용자의 단일 Request를 처리하기 위한 내부 실행 절차다.
* Agent Loop는 Project, Criteria, Project State, Artifact를 직접 변경하지 않는다.
* Agent Loop는 허용된 ToolHost 도구만 호출할 수 있다.
* MVP의 도구는 읽기 전용 도구로 제한한다.
* 상태 변경, 기준 변경, 산출물 확정, 파일 수정, 외부 실행은 사용자 승인 없이는 수행할 수 없다.

## 실행 흐름 (DEC-048)

```text
0 사용자 발화
→ 1 LLM이 list_tools 요청
→ 2 IDE가 가용 도구 목록(tool_list) 제공
→ 3 LLM tool_request
→ 4 IDE 도구 실행 및 결과(tool_result) 제공
→ 5 LLM 정리·완료 판단 (3부터 반복)
→ 6 final 종료
```

## 구현 항목

* Core: `IAgentLoop`/`AgentLoop`, `IChatModelClient`, `IAgentTool`/`IAgentToolHost`/`AgentToolHost`
* Core: 모델(AgentTurn, AgentToolRequest/Result, AgentRunRequest/Result, StopReason, AgentLoopOptions)
* 프로토콜: `list_tools` / `tool_list` / `tool_request` / `tool_result` / `final` (AgentProtocol, AgentMessageParser)
* 도구 자기기술: `Name` / `Description` / `Arguments`
* 도구: `fetch_url`(SSRF 차단), `web_search`(키 없는 DuckDuckGo HTML), `read_file`(프로젝트 폴더 내 읽기, 경로 탈출 차단), `list_files`(프로젝트 폴더 내 폴더/파일 목록, `.llmide` 제외), `artifacts_list`/`read_artifact`(아티팩트 목록·조회, ArtifactService 경유)
* 도구 등록·필터 일원화: `AgentToolHostFactory`
* 감독 allowlist: 전역 `<IdeProgramRoot>/settings/agent-tools.json`
* 도구 호출 로그: `conversations/tool-calls.jsonl` + SQLite `agent_tool_calls` (DEC-050)
* DeepSeek 어댑터: `DeepSeekChatModelClient`
* CLI: `agent-run` 명령
* 채팅 통합: `ChatService`가 동일 프로토콜로 도구 호출, WPF는 도구 단계 라벨 표시

## 보안 기준 (DEC-046)

* 허용 스킴 http/https, 차단 file/ftp/data/javascript 등
* 차단 호스트/대역: localhost, 0.0.0.0, 루프백, 사설망, link-local, unique-local, 빈/과장 URL
* 응답 크기·문자 수·리다이렉트·타임아웃 제한
* 로그에는 전체 URL/시크릿을 남기지 않고 host 정도만 남긴다
* 도구 실패는 앱을 죽이지 않고 실패 ToolResult로 반환한다
* 테스트는 실제 외부 URL을 호출하지 않는다 (가짜 HTTP 핸들러 사용)

## 완료 기준

* 사용자 단일 Request에서 LLM과 도구가 다회차로 주고받아 최종 답변을 만든다.
* LLM은 URL을 직접 열지 않고 도구를 통해서만 외부에 접근한다.
* 도구 목록은 list_tools 디스커버리로 제공되고, 감독이 전역 allowlist로 제한할 수 있다.
* 도구는 읽기 전용이며, 상태/기준/산출물/파일을 직접 변경하지 않는다.
* CLI `agent-run`과 WPF 채팅 모두 Agent Loop를 사용한다.
* 빌드 경고 0/오류 0, 테스트 전체 통과.

## 후속 개선 (v0.5+)

Phase 8.5 완료 이후, 검색·도구 사용 안정화와 진단을 위해 다음을 반영했다. 세부는 Decision Log 참조.

* 진단 로깅: LLM 요청/응답 전체 `[Info]`, 도구 I/O 전체 `[Debug]`를 SystemLog에 기록. (DEC-057)
* LLM 주입 지시문을 `policies/prompts.json`으로 통합, 기존 `tool-rule.md` 제거. (DEC-058)
* 채팅 도구 호출을 `requests` 배열로 강제 + 병렬 실행 + 부분 실패 허용. (DEC-059)
* 모델 응답 JSON 추출을 "첫 균형 객체"로 견고화(여분 `}`/래퍼 태그/다중 객체 대응). (DEC-060)
* `web_search` DuckDuckGo POST 전환(GET 차단 → 결과 정상 수신). (DEC-061)
* 파일 읽기 도구 `read_file` 추가: 프로젝트 폴더(WorkingDirectory) 하위만 읽기. 절대경로·`..` 상위 이동·프로젝트 밖·시스템 폴더(`.llmide`) 접근은 보안 위반으로 차단. (DEC-063)
* 폴더/파일 목록 도구 `list_files` 추가: 프로젝트 폴더 하위 목록(선택 재귀). read_file과 동일 경로 가드(`ProjectPathGuard`로 일원화), `.llmide`는 목록·재귀에서도 제외. (DEC-064)
* 아티팩트 도구 `artifacts_list`(목록)·`read_artifact`(ID로 내용 조회) 추가: `.llmide/artifacts`를 raw 파일 접근이 아니라 `ArtifactService`로 노출. (DEC-065)

## 상태

```text
완료
```

---

---

# Phase 9. WPF GUI 적용

## 목표

CLI에서 검증된 Core 기능을 WPF에서 사용할 수 있게 한다.

WPF는 Core를 호출하는 UI 계층이다.
비즈니스 로직은 WPF에 두지 않는다.

## 구현 항목

* `LlmIde.Wpf` 프로젝트 생성
* MainWindow 생성
* Project Registry / Project Explorer
* 프로젝트 생성, 이름 변경, 복제, 삭제
* API Key 입력을 포함한 프로젝트 생성 다이얼로그
* 장기 대화 / 1회성 대화 선택
* Chat 화면
* Chat Streaming View
* 독립 팝업 채팅 입력창
* Provider 선택
* Criteria / 정책 룰 편집
* 대화 목록 표시
* 대화 중요도 표시 및 수정
* 대화 삭제
* 수동 대화 추가
* Artifact 자동 저장
* Artifact 목록 / 뷰어 / 편집 / 삭제
* 응답 선택 영역 Artifact 추출
* Agent 도구 관리 화면
* Markdown / 코드 뷰어 적용
* 도구 실행 상태 표시

## 완료 기준

* Core 로직 수정 없이 WPF가 기존 프로젝트 데이터를 읽을 수 있다.
* WPF에서 프로젝트를 생성, 선택, 복제, 삭제할 수 있다.
* WPF에서 chat을 실행할 수 있다.
* WPF에서 스트리밍 응답을 확인할 수 있다.
* WPF에서 Criteria와 정책 파일을 편집할 수 있다.
* WPF에서 대화 목록, 중요도, 삭제 기능을 사용할 수 있다.
* WPF에서 Artifact를 저장, 조회, 편집, 삭제할 수 있다.
* WPF에서 Agent 도구 목록을 확인할 수 있다.
* WPF와 CLI가 동일한 `.llmide` 데이터를 사용한다.

## 상태

```text
완료
```

## 비고

Phase 9는 기본 WPF IDE 형태를 완성한 단계로 종료한다.

이후 GUI 수정은 별도 Phase의 완료 조건을 막는 항목으로 보지 않는다.
Provider / Model 설정 보강, Project State 편집 UI, Rolling Context / Context Package / Request Log Viewer 등은 후속 제품 고도화 작업으로 이동한다.

---

# Phase 10. AI Writing Assistant 방향 전환

## 목표

LLM IDE2의 1차 제품 시나리오를 **AI 글쓰기 도우미**로 재정의한다.

LLM IDE2는 범용 자율 개발 에이전트가 아니라, 프로젝트 기억과 읽기 전용 Agent Tool을 활용해 글쓰기, 연구 메모, 논문 초안, 기술 문서, 블로그 글, README 작성 등을 돕는 **프로젝트 기반 AI 글쓰기 작업실**을 우선 목표로 한다.

## 제품 방향

```text
LLM IDE2
= Project Memory Runtime
+ Writing Workspace
+ Research Assistant
+ Artifact-based Draft Manager
```

사용자는 프로젝트 폴더 안에 원고, 메모, 참고자료, 초안, URL, 아티팩트를 보관한다.

IDE는 다음을 관리한다.

* 프로젝트 목표
* 글쓰기 목표
* 현재 작성 단계
* 현재 섹션
* 대화 로그
* 롤링 맥락 요약
* 아티팩트
* 프로젝트 파일
* 웹 조사 결과
* 요청별 맥락 패키지
* 도구 호출 이력

AI는 다음을 수행한다.

* 자료 조사
* URL 본문 읽기
* 프로젝트 파일 읽기
* 저장된 아티팩트 읽기
* 개요 작성
* 문단 초안 작성
* 논리 구조 점검
* 표현 개선
* 요약
* 리뷰
* 수정안 제안

## 기본 도구 범위

Phase 10 기준 Agent 도구는 읽기 전용으로 유지한다.

```text
web_search
- 실시간 웹 검색

fetch_url
- URL 본문 가져오기

read_file
- 프로젝트 폴더 내 파일 읽기
- 상대경로만 허용
- .llmide 직접 접근 차단

list_files
- 프로젝트 폴더 내 파일/폴더 목록 조회
- .llmide 제외

artifacts_list
- 저장된 아티팩트 목록 조회

read_artifact
- 아티팩트 ID로 내용 조회
```

## 구현 항목

* 제품 설명을 AI 글쓰기 도우미 중심으로 갱신
* README의 제품 방향 수정
* Product Principles에 Writing Assistant Principle 추가
* Project State를 글쓰기 프로젝트 기준으로 확장
* WPF에 Writing Workspace 개념 도입
* Artifact type에 글쓰기용 유형 정리
* 기본 정책 프롬프트를 글쓰기/연구 보조에 맞게 조정
* Agent Tool 설명을 글쓰기 작업 흐름 기준으로 정리

## 완료 기준

* 프로젝트의 1차 사용 시나리오가 AI 글쓰기 도우미로 문서화된다.
* 기존 Core / WPF 구조를 유지하면서 글쓰기 작업 흐름으로 설명할 수 있다.
* Agent Tool은 읽기와 조사 중심으로 유지된다.
* 파일 수정, 자동 커밋, 외부 명령 실행은 여전히 범위 밖으로 둔다.
* Artifact가 글쓰기 초안, 문단, 개요, 요약, 리뷰 결과를 저장하는 핵심 단위로 정리된다.

## 상태

```text
다음 작업
```

---

# Phase 11. Writing Project State / Plan

## 목표

Project State를 기능 시험 기록이 아니라 **글쓰기 프로젝트의 작업 지휘판**으로 재정의한다.

Project State는 토큰 사용량, Context Package 크기, Compression 상태 같은 런타임 진단 정보가 아니다.
Project State는 사용자가 승인한 글쓰기 목표, 현재 단계, 현재 섹션, 할 일, 결정 사항을 관리한다.

## Project State 필드 후보

```json
{
  "project_goal": "",
  "writing_goal": "",
  "document_type": "",
  "target_audience": "",
  "current_stage": "",
  "current_section": "",
  "current_task": "",
  "completed_items": [],
  "in_progress_items": [],
  "next_actions": [],
  "blockers": [],
  "last_decision": "",
  "updated_at": ""
}
```

## 필드 의미

```text
project_goal
- 프로젝트 전체 목표

writing_goal
- 작성하려는 글의 목표

document_type
- article, paper, thesis, blog, README, report 등

target_audience
- 대상 독자

current_stage
- research, outline, drafting, revision, finalizing 등

current_section
- 현재 작업 중인 장/절/문단

current_task
- 지금 수행 중인 구체 작업

completed_items
- 완료한 작업

in_progress_items
- 진행 중인 작업

next_actions
- 다음 할 일

blockers
- 막힌 점

last_decision
- 마지막 확정 결정
```

## 구현 항목

* `project-state.json` 스키마 확장
* 기존 Project State와 하위 호환
* WPF Project State / Plan Editor 추가
* Project Goal 입력
* Writing Goal 입력
* Document Type 입력 또는 선택
* Target Audience 입력
* Current Stage 입력 또는 선택
* Current Section 입력
* Current Task 입력
* Completed / In Progress / Next Actions / Blockers 편집
* Last Decision 편집
* Chat 요청에 Project State를 글쓰기 맥락으로 주입

## 완료 기준

* 사용자가 글쓰기 목표와 현재 작업 단계를 명시적으로 저장할 수 있다.
* AI가 응답할 때 현재 글쓰기 목표, 독자, 문서 유형, 현재 섹션을 참고할 수 있다.
* 기능 테스트 기록과 글쓰기 프로젝트 상태가 섞이지 않는다.
* Project State는 사용자가 직접 편집하고 승인한 상태로 유지된다.

## 상태

```text
대기
```

---

# Phase 12. Research / Source Workflow

## 목표

AI가 글쓰기와 논문 작성에 필요한 자료를 조사하고, 프로젝트 내부 자료와 외부 웹 자료를 읽어 요약할 수 있게 한다.

이 단계는 RAG를 구현하는 단계가 아니다.
읽기 전용 Agent Tool을 활용해 사용자가 요청한 자료 조사와 요약을 수행하는 단계다.

## 기본 흐름

```text
사용자 요청
→ web_search
→ fetch_url
→ 요약 / 비교 / 정리
→ 필요한 경우 Artifact로 저장
```

프로젝트 내부 자료를 사용할 경우:

```text
사용자 요청
→ list_files
→ read_file
→ 요약 / 인용 후보 / 논점 추출
→ Artifact로 저장
```

저장된 산출물을 사용할 경우:

```text
사용자 요청
→ artifacts_list
→ read_artifact
→ 재구성 / 비교 / 확장 / 요약
```

## 구현 항목

* Research 요청용 기본 프롬프트 정리
* 검색 결과 요약 형식 정리
* URL 본문 요약 형식 정리
* 참고자료 요약 Artifact type 추가
* Source Note Artifact type 추가
* Literature Note Artifact type 추가
* 웹 검색 결과에서 중요한 URL을 다시 fetch하도록 유도
* 조사 결과의 출처 URL을 응답에 보존
* WPF에서 도구 호출 상태와 결과 요약 표시 개선
* 도구 호출 로그 조회 보강

## Artifact type 후보

```text
SourceNote
LiteratureNote
ReferenceSummary
ResearchMemo
ReadingNote
QuoteCandidate
```

## 완료 기준

* 사용자가 웹 검색을 통해 글쓰기 자료를 조사할 수 있다.
* 사용자가 URL 본문을 가져와 요약할 수 있다.
* 사용자가 프로젝트 폴더 내 파일을 읽어 요약할 수 있다.
* 사용자가 저장된 아티팩트를 다시 읽어 글쓰기 맥락으로 활용할 수 있다.
* 조사 결과를 Artifact로 저장할 수 있다.
* 출처 URL과 요약 내용이 분리되어 보존된다.
* 도구는 읽기 전용으로 유지된다.

## 상태

```text
대기
```

---

# Phase 13. Draft / Artifact Writing Workflow

## 목표

Artifact를 글쓰기 초안 관리의 중심 단위로 사용한다.

AI 응답 전체를 흘려보내지 않고, 개요, 문단, 섹션, 요약, 리뷰, 수정안 등 보존 가치가 있는 글 조각을 Artifact로 저장하고 재사용한다.

## 기본 흐름

```text
대화
→ 아이디어 탐색

Artifact
→ 저장할 글 조각

Project State
→ 현재 글쓰기 목표와 단계

Agent Tools
→ 자료 조사와 읽기

WPF
→ 작성 작업 공간
```

## 구현 항목

* 글쓰기용 Artifact type 정리
* Artifact 목록 필터링
* Artifact type별 뷰어 개선
* Artifact 편집 UI 개선
* Artifact 제목 / 유형 / 출처 대화 ID 표시 개선
* Artifact를 현재 글쓰기 작업에 첨부
* 여러 Artifact를 조합해 초안 생성
* 개요 Artifact 생성
* 섹션 초안 Artifact 생성
* 문단 초안 Artifact 생성
* 리뷰 결과 Artifact 생성
* 수정안 Artifact 생성
* 선택한 Artifact를 Markdown으로 복사
* 선택한 Artifact를 파일로 내보내기

## 글쓰기용 Artifact type 후보

```text
Outline
Draft
SectionDraft
ParagraphDraft
Revision
Review
Summary
ResearchMemo
SourceNote
CitationNote
FinalDraft
```

## 완료 기준

* 사용자는 AI 응답에서 글 조각을 Artifact로 저장할 수 있다.
* 사용자는 저장된 Artifact를 다시 읽고 수정할 수 있다.
* AI는 저장된 Artifact를 읽어 다음 초안에 반영할 수 있다.
* 여러 Artifact를 조합해 긴 초안을 만들 수 있다.
* 사용자는 Artifact를 Markdown 파일로 내보낼 수 있다.
* 원본 대화 로그와 Artifact는 분리되어 보존된다.

## 상태

```text
대기
```

---

# Phase 14. Writing Review / Revision Workflow

## 목표

AI가 초안의 논리, 구조, 표현, 일관성, 누락 지점을 검토하고 수정안을 제안할 수 있게 한다.

이 단계에서도 AI는 파일을 직접 수정하지 않는다.
AI는 리뷰와 수정안을 제안하고, 사용자가 선택해 Artifact 또는 파일로 저장한다.

## 리뷰 유형

```text
Structure Review
- 글의 전체 구조 점검

Logic Review
- 주장, 근거, 결론의 연결성 점검

Clarity Review
- 문장 명확성 점검

Style Review
- 문체, 톤, 독자 적합성 점검

Citation Review
- 참고자료 사용 여부와 출처 누락 점검

Gap Review
- 빠진 논점, 보강할 부분 제안

Revision Proposal
- 수정안 제안
```

## 구현 항목

* Review용 프롬프트 정리
* 선택한 Artifact 리뷰
* 선택한 파일 리뷰
* 현재 Project State 기준 리뷰
* Criteria 기준 리뷰
* 리뷰 결과 Artifact 저장
* 수정안 Artifact 저장
* Before / After 형식 출력
* 수정 이유와 수정문 분리
* WPF에서 리뷰 결과 보기 개선

## 완료 기준

* 사용자는 초안 Artifact를 AI에게 리뷰시킬 수 있다.
* 사용자는 프로젝트 파일을 AI에게 리뷰시킬 수 있다.
* AI는 수정안을 직접 파일에 적용하지 않고 제안한다.
* 사용자는 리뷰 결과와 수정안을 Artifact로 저장할 수 있다.
* 글쓰기 목표, 대상 독자, 문서 유형이 리뷰 기준으로 사용된다.

## 상태

```text
대기
```

---

# Phase 15. Writing File Workflow

## 목표

프로젝트 폴더 안의 원고, 메모, 참고자료 파일을 글쓰기 맥락으로 활용한다.

기존 `read_file`, `list_files` 도구는 이미 프로젝트 폴더 내부 파일 읽기를 지원한다.
이 단계에서는 이를 글쓰기 작업 흐름에 맞게 정리하고, 명시적 파일 내보내기 기능을 추가한다.

## 원칙

* AI는 프로젝트 파일을 읽을 수 있다.
* AI는 파일을 직접 수정하지 않는다.
* 파일 쓰기는 사용자가 명시적으로 실행한 내보내기 기능으로만 수행한다.
* `.llmide` 내부 파일은 raw file 도구로 읽지 않는다.
* ArtifactService를 통한 Artifact 조회는 허용한다.

## 구현 항목

* 프로젝트 파일 목록 패널 개선
* 원고 / 메모 / 자료 파일 구분 표시
* 파일 읽기 요청 UX 정리
* 파일 기반 요약 요청 UX 정리
* Artifact를 Markdown 파일로 내보내기
* FinalDraft Artifact를 파일로 저장
* 파일 저장 전 확인 다이얼로그
* 동일 파일 존재 시 덮어쓰기 확인
* 저장 이력 기록

## 완료 기준

* 사용자는 프로젝트 폴더 파일 목록을 확인할 수 있다.
* AI는 사용자가 요청한 프로젝트 파일을 읽고 요약할 수 있다.
* 사용자는 Artifact를 명시적으로 파일로 내보낼 수 있다.
* AI가 사용자 승인 없이 파일을 수정하지 않는다.
* 파일 내보내기는 추적 가능하게 기록된다.

## 상태

```text
대기
```

---

# Phase 16. Writing State Candidate

## 목표

AI가 글쓰기 프로젝트 상태 변경 후보를 제안하고, 사용자가 승인한 내용만 Project State에 반영한다.

State Update Candidate를 일반 프로젝트 관리 기능이 아니라 글쓰기 프로젝트 관리 기능으로 재정의한다.

## 후보 예시

```text
현재 단계 변경
- research → outline
- outline → drafting
- drafting → revision

현재 섹션 변경
- Introduction
- Related Work
- Method
- Discussion

완료 항목 추가
- 선행연구 3건 요약 완료
- 1장 초안 작성 완료

다음 작업 제안
- 관련 연구 비교표 작성
- 결론 보강
- 참고 URL 정리

Blocker 제안
- 출처 부족
- 논리 연결 약함
- 대상 독자 불명확
```

## 구현 항목

* State Candidate 생성 프롬프트 정리
* 최근 대화와 Artifact 기준 후보 생성
* 후보 목록 조회
* 후보 상세 조회
* 후보 수정
* 후보 승인
* 후보 거부
* 승인된 후보만 Project State에 반영
* 적용 이력 저장
* WPF 승인 화면 구현

## 완료 기준

* AI는 글쓰기 상태 변경 후보만 제안한다.
* 후보는 자동 적용되지 않는다.
* 사용자가 승인한 내용만 Project State에 반영된다.
* Project State가 현재 글쓰기 목표와 진행 상황을 정확히 반영한다.

## 상태

```text
대기
```

---

# Phase 17. Local Server 적용

## 목표

CLI와 GUI 외부의 로컬 프로그램이 LLM IDE 기능을 호출할 수 있게 한다.

AI 글쓰기 도우미 기능을 외부 편집기, 로컬 자동화 도구, 별도 프론트엔드에서 호출할 수 있게 한다.

## 구현 항목

* `LlmIde.Server` 프로젝트 추가
* 로컬 TCP 서버 구현
* 로컬 UDP 서버 구현
* 로컬 HTTP API 적용 여부 검토
* 서버 시작/중지 명령
* 서버 포트 설정
* Project 목록/열기 API
* Chat 요청 API
* Streaming Chat 응답 API
* Artifact 조회 API
* Artifact 저장 API
* Project State 조회 API
* Agent Tool 호출 이력 조회 API
* 로컬 접근 제한 및 보안 정책

## 완료 기준

* Core 로직 수정 없이 Server 계층을 적용할 수 있다.
* CLI, WPF, Server가 동일한 Core 서비스를 사용한다.
* 외부 로컬 프로그램이 글쓰기 프로젝트 기능을 호출할 수 있다.
* 요청은 기존 Conversation Log와 Context Package에 기록된다.
* 활성 Criteria, Project State, Rolling Context, Artifact가 서버 요청에도 동일하게 적용된다.

## 상태

```text
후속
```

---

# Roadmap Summary

현재 개발 상태는 다음과 같다.

```text
완료:
Phase 1 Project
Phase 2 Provider
Phase 3 Conversation
Phase 4 Criteria
Phase 5 Project State
Phase 6 Rolling Context Compression
Phase 7 Context Builder v1
Phase 8 Artifact
Phase 8.5 Agent Loop v1
Phase 9 WPF GUI

다음:
Phase 10 AI Writing Assistant 방향 전환

후속:
Phase 11 Writing Project State / Plan
Phase 12 Research / Source Workflow
Phase 13 Draft / Artifact Writing Workflow
Phase 14 Writing Review / Revision Workflow
Phase 15 Writing File Workflow
Phase 16 Writing State Candidate
Phase 17 Local Server
```

한 줄 요약:

```text
LLM IDE2는 Core/CLI/WPF 기반을 완성했고, 이후 단계에서는 프로젝트 기억과 읽기 전용 Agent Tool을 활용하는 AI 글쓰기 작업실로 제품 방향을 구체화한다.
```


## 5. Codex 작업 목록

### Codex 작업 005. 프로젝트 상태 관리 구현

상태:

```text
완료
```

구현 내용:

* `project-state.json` 저장소 구현
* stage, current task, completed items, next actions, blockers 관리
* CLI state 명령 구현
* chat 요청에 프로젝트 상태 포함

---

### Codex 작업 006. Rolling Context 저장소 구조 생성

상태:

```text
완료
```

구현 내용:

* `conversations/rolling-context/current.md` 생성
* `conversations/rolling-context/history/` 생성
* `conversations/rolling-context/index.jsonl` 생성
* SQLite `rolling_context_summaries` 테이블 추가

---

### Codex 작업 007. 압축 규칙 및 context-policy 확장

상태:

```text
완료
```

구현 내용:

* `policies/compression-rule.md` 생성
* `context-policy.json`에 요약 + 창 정책 추가
* compression failure strategy 적용

---

### Codex 작업 008. 압축 요청 생성기 구현

상태:

```text
완료
```

구현 내용:

* 이전 롤링 맥락 요약 로드
* 현재 사용자/AI 메시지 수집
* 압축 규칙 기반 내부 요청 생성
* Provider API로 Summary 생성

---

### Codex 작업 009. chat 후 롤링 맥락 요약 갱신 구현

상태:

```text
완료
```

구현 내용:

* chat 응답 저장 후 압축 요청 실행
* 새 Summary를 history에 저장
* `current.md` 갱신
* 실패 시 기존 Summary 유지

---

### Codex 작업 010. 맥락 빌더 v1에서 Rolling Summary + 최근 대화 창 주입

상태:

```text
완료
```

구현 내용:

* 롤링 맥락 요약 포함
* 최근 대화 창는 MVP에서 0으로 기록
* 전체 과거 원문 제외
* 맥락 패키지에 사용된 맥락 기록

---

### Codex 작업 011. Request Log에 used_rolling_context_id 및 compression_status 기록

상태:

```text
진행 확인 필요
```

구현 내용:

* 채팅 요청와 압축 요청 구분
* `used_rolling_context_id` 기록
* `compression_status` 기록
* `compression_error` 기록

---

## 6. 다음 Codex 작업 후보

### Codex 작업 012. WPF 프로젝트 및 Shell 생성

목표:

* `LlmIde.Wpf` 프로젝트를 생성하고 기본 Shell을 구성한다.

요구사항:

* WPF 프로젝트 생성
* 기존 Core 및 Infrastructure 참조
* `MainWindow` 생성
* 기본 레이아웃 구성
* 프로그램 루트 확인
* CLI와 동일한 `.llmide` 데이터 접근 준비

완료 기준:

* WPF 앱이 실행된다.
* Core 로직을 WPF에서 호출할 수 있다.
* 기존 프로젝트 등록소를 읽을 수 있다.

---

### Codex 작업 013. Project Registry 및 Chat 화면 연결

목표:

* WPF에서 프로젝트 선택과 chat 실행을 가능하게 한다.

요구사항:

* Project Registry 목록 표시
* 프로젝트 선택 및 열기
* Provider 및 Model 표시
* Chat 입력창
* Chat 응답 표시
* 스트리밍 응답 표시

완료 기준:

* WPF에서 기존 프로젝트를 선택할 수 있다.
* WPF에서 chat을 실행할 수 있다.
* CLI와 WPF가 같은 대화 로그를 사용한다.

---

### Codex 작업 014. 관리 화면 1차 연결

목표:

* WPF에서 CLI로 검증된 관리 데이터를 조회하고 편집한다.

요구사항:

* Criteria 목록 및 편집
* 프로젝트 상태 표시 및 편집
* 산출물 목록 및 상세 보기
* Rolling Context Summary 보기
* 맥락 패키지 보기
* Request Log 보기

완료 기준:

* WPF에서 Criteria를 조회하고 수정할 수 있다.
* WPF에서 프로젝트 상태를 조회하고 수정할 수 있다.
* WPF에서 산출물와 롤링 맥락을 확인할 수 있다.
* WPF에서 요청 로그와 맥락 패키지를 확인할 수 있다.

---

### Codex 작업 015. 대화 저장 ID 순번화

목표:

* 대화 저장 ID를 GUID 기반 값에서 프로젝트별 순번 기반 값으로 변경한다.

요구사항:

* request_id는 저장 순서대로 `1`, `2`, `3`, `4` 형태로 증가한다.
* message_id는 저장 순서대로 `1`, `2`, `3`, `4` 형태로 증가한다.
* JSONL, SQLite, context package 파일명이 같은 request_id를 공유한다.
* 기존 GUID 기반 기록은 유지하고, 신규 숫자 ID 생성 시 숫자 ID만 순번 계산에 사용한다.

완료 기준:

* 첫 chat 요청은 request_id `1`로 저장된다.
* 첫 chat 후 compression 요청은 request_id `2`로 저장된다.
* 첫 user 메시지는 message_id `1`로 저장된다.
* 첫 assistant 메시지는 message_id `2`로 저장된다.
* 이후 대화는 기존 숫자 ID의 다음 순번으로 저장된다.

상태:

```text
완료
```

---

### Codex 작업 016. 대화 중요도 가중치 저장

목표:

* chat 응답에서 현재 대화의 중요도를 LLM에게 평가받고 대화 데이터에 저장한다.

요구사항:

* `importance_weight` 값 범위는 `0~10`이다.
* 숫자가 커질수록 중요도가 높다.
* 중요도 측정 명령은 코드 상수가 아니라 `policies/importance-rule.md` 파일에 둔다.
* 신규 프로젝트 생성 시 `.llmide/policies/importance-rule.md`를 생성한다.
* chat 요청에 importance rule을 system 메시지로 포함한다.
* LLM 응답의 명시 태그에서 중요도를 파싱한다.
* 파싱한 중요도를 chat request record에 저장한다.

완료 기준:

* `conversation_turns`와 `requests.jsonl`의 chat request record에 `importance_weight`가 저장된다.
* 중요도 태그가 없거나 파싱할 수 없으면 `0`으로 저장된다.
* compression request의 기본 중요도는 `0`이다.
* 중요도 측정 프롬프트 문구는 `importance-rule.md` 파일에서 관리된다.

상태:

```text
완료
```

---

### Codex 작업 017. 프로젝트 등록소 pID / name 분리

목표:

* 프로젝트 등록소의 내부 키와 사용자 표시 이름을 분리한다.

요구사항:

* 기존 registry의 `name` 키 역할은 `pID`로 변경한다.
* `pID`는 영어 기반 프로젝트 키로 사용한다.
* `name`은 사용자 지정 표시 이름으로 별도 저장한다.
* CLI에서 프로젝트 지정 인자는 `pID`로 해석한다.
* `projects rename`은 표시 이름을 변경한다.
* `projects repid`는 프로젝트 `pID`를 변경한다.
* 기존 `name`만 있는 registry 항목은 읽기 호환한다.

완료 기준:

* 신규 `projects.json` 항목은 `pID`, `name`, `path`, `created_at`을 저장한다.
* `pID`는 영문자로 시작하고 영문자, 숫자, `-`, `_`만 허용한다.
* 사용자는 표시 이름 `name`을 자유롭게 지정하고 수정할 수 있다.
* 기존 CLI 명령은 `pID`로 프로젝트를 찾는다.

상태:

```text
완료
```

---

## 7. MVP 완료 기준

MVP는 다음 조건을 만족해야 한다.

* 긴 대화에서도 과거 전체 원문을 매 요청에 포함하지 않는다.
* 과거 맥락은 롤링 맥락 요약로 전달한다.
* 최근 대화는 최근 대화 창로 전달한다.
* 롤링 맥락 요약는 LLM Provider API를 통해 생성한다.
* 원본 대화 로그와 압축 맥락은 별도로 보존한다.
* Compression 실패 시 원본 대화과 사용자 응답은 유지한다.
* 각 요청에서 어떤 압축 맥락이 사용되었는지 추적할 수 있다.
* `요약 + 창`를 기본 맥락 관리 전략으로 사용한다.
* 프로젝트 상태를 저장하고 요청에 포함할 수 있다.
* Criteria를 저장하고 요청에 포함할 수 있다.
* 산출물를 저장하고 요청에 첨부할 수 있다.
* CLI와 WPF는 동일한 Core를 사용할 수 있다.
* WPF에서 현재 Core/CLI 기반 기능을 사용할 수 있다.
* Context Notes를 저장하고 요청에 포함할 수 있다.
* File Store를 통해 파일을 명시적으로 요청에 첨부할 수 있다.
* State Update Candidate는 사용자 승인 후에만 적용된다.
* Server는 후속 어댑터 계층으로 Core를 호출할 수 있다.

---

## 8. Roadmap 운영 원칙

로드맵은 실행 계획이다.

다음 항목은 로드맵에 직접 섞지 않고 별도 문서에서 관리한다.

```text
제품 원칙
아키텍처 기준
결정 이력
변경 이력
아이디어 후보
```

로드맵 변경 규칙:

```text
작은 작업 추가
→ Roadmap에 반영

구조 변경
→ Decision Log에 먼저 기록

기준선 변경
→ 아키텍처 기준 갱신

제품 원칙 변경
→ Product Principles 갱신

개발 진행 변경
→ Roadmap 갱신
```

---

## 9. Roadmap Summary

현재 개발 상태는 다음과 같다.

```text
완료:
Phase 1 Project
Phase 2 Provider
Phase 3 Conversation
Phase 4 Criteria
Phase 5 프로젝트 상태
Phase 6 Rolling Context Compression
Phase 7 맥락 빌더 v1
Phase 8 산출물
Phase 8.5 Agent Loop v1 (Search and Fetch Tools)
Phase 9 WPF GUI

다음:
Phase 10 AI Writing Assistant 방향 전환

후속:
Phase 11 Writing Project State / Plan
Phase 12 Research / Source Workflow
Phase 13 Draft / Artifact Writing Workflow
Phase 14 Writing Review / Revision Workflow
Phase 15 Writing File Workflow
Phase 16 Writing State Candidate
Phase 17 Local Server
```

한 줄 요약:

```text
LLM IDE2는 Core/CLI/WPF 기반을 완성했고, 이후 단계에서는 프로젝트 기억과 읽기 전용 Agent Tool을 활용하는 AI 글쓰기 작업실로 제품 방향을 구체화한다.
```
